node_modules/

# Root npm metadata / scripts — kept on the tooling branch, not main
/package.json
/package-lock.json
/scripts/

# Build output (node site/build.mjs) — deploy / docker nginx root
site/dist/
